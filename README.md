

<div dir="rtl">

# 🥗 دستیار هوشمند غذا و رستوران

این پروژه یک وب اپلیکیشن هوشمند است که با استفاده از معماری **RAG (Retrieval-Augmented Generation)** به سوالات کاربران در مورد غذا، رستوران و دستور پخت‌ها پاسخ می‌دهد. دانش این دستیار به صورت پویا از لینک‌های وبی که کاربر ارائه می‌دهد، تامین می‌شود.

## ✨ ویژگی‌ها

-   **پایگاه دانش پویا:** اطلاعات و دانش سیستم از لینک‌های وبی که کاربر در لحظه وارد می‌کند، ساخته می‌شود.
-   **سیستم پرسش و پاسخ هوشمند:** با استفاده از مدل‌های زبانی بزرگ (LLM)، پاسخ‌هایی دقیق و مرتبط با سوال کاربر تولید می‌کند.
-   **نمایش منابع:** برای هر پاسخ، منابع و بخش‌هایی از متن که برای تولید آن پاسخ استفاده شده‌اند، نمایش داده می‌شود تا اعتبار پاسخ‌ها تضمین شود.
-   **رابط کاربری کاملاً فارسی:** با استفاده از Streamlit و CSS سفارشی، یک تجربه کاربری روان، زیبا و کاملاً راست‌چین برای زبان فارسی فراهم شده است.
-   **اجرای محلی:** با بهره‌گیری از **Ollama**، مدل زبانی بزرگ (Llama 3) به صورت کاملاً محلی (Local) اجرا می‌شود و نیازی به API Key یا اینترنت پرسرعت ندارد.

## ⚙️ معماری و نحوه کار

این پروژه بر اساس معماری **RAG** کار می‌کند که شامل سه مرحله اصلی است:

1.  **یادگیری (Ingestion):**
    -   کاربر لینک‌های مورد نظر خود را وارد می‌کند.
    -   `WebBaseLoader` محتوای متنی صفحات وب را استخراج می‌کند.
    -   `RecursiveCharacterTextSplitter` متون طولانی را به قطعات کوچک‌تر (Chunks) تقسیم می‌کند.
    -   `HuggingFaceEmbeddings` هر قطعه متن را به یک بردار عددی (Vector) تبدیل می‌کند که نماینده معنای آن متن است.
    -   `ChromaDB` این بردارها را در یک پایگاه داده برداری ذخیره می‌کند.

2.  **بازیابی (Retrieval):**
    -   کاربر سوال خود را مطرح می‌کند.
    -   سوال کاربر نیز به بردار عددی تبدیل می‌شود.
    -   `ChromaDB` با مقایسه بردار سوال و بردارهای موجود، مرتبط‌ترین قطعات متنی (Chunks) را پیدا و بازیابی می‌کند.

3.  **تولید (Generation):**
    -   قطعات متنی بازیابی شده به عنوان **کانتکست (Context)** به همراه سوال اصلی کاربر، در یک **پرامپت (Prompt)** دقیق قرار می‌گیرند.
    -   این پرامپت به مدل زبانی بزرگ (`Llama 3` که از طریق `Ollama` اجرا شده) ارسال می‌شود.
    -   مدل، با اتکا به کانتکست ارائه‌شده، یک پاسخ دقیق و روان به زبان فارسی تولید می‌کند.

## 🛠️ تکنولوژی‌های استفاده شده

-   **رابط کاربری (Frontend):** [Streamlit](https://streamlit.io/)
-   **ارکستراسیون (Orchestration):** [LangChain](https://www.langchain.com/)
-   **مدل زبانی بزرگ (LLM):** [Ollama](https://ollama.com/) (با مدل Llama 3)
-   **پایگاه داده برداری (Vector Database):** [ChromaDB](https://www.trychroma.com/)
-   **مدل Embedding:** [Hugging Face Sentence-Transformers](https://huggingface.co/sentence-transformers)
-   **کار با داده:** [Pandas](https://pandas.pydata.org/)

## 🚀 راه‌اندازی و نصب

برای اجرای این پروژه به صورت محلی، مراحل زیر را دنبال کنید:

### پیش‌نیازها

1.  **Python 3.8 یا بالاتر**
2.  **نصب Ollama:**
    -   [Ollama را دانلود و نصب کنید.](https://ollama.com/)
    -   پس از نصب، دستور زیر را در ترمینال خود اجرا کنید تا مدل Llama 3 دانلود شود:
        ```bash
        ollama pull llama3
        ```

### مراحل نصب

1.  **کلون کردن ریپازیتوری:**
    ```bash
    git clone https://github.com/your-username/your-repository-name.git
    cd your-repository-name
    ```

2.  **ساخت محیط مجازی (پیشنهادی):**
    ```bash
    python -m venv venv
    # On Windows
    venv\Scripts\activate
    # On macOS/Linux
    source venv/bin/activate
    ```

3.  **نصب کتابخانه‌های مورد نیاز:**
    ابتدا یک فایل به نام `requirements.txt` بسازید و محتوay زیر را در آن قرار دهید:

    ```txt
    streamlit
    pandas
    langchain-community
    langchain-huggingface
    langchain-text-splitters
    chromadb
    ollama
    ```

    سپس دستور زیر را برای نصب اجرا کنید:
    ```bash
    pip install -r requirements.txt
    ```

4.  **اجرای برنامه:**
    فایل اصلی پایتون خود را (مثلاً `app.py`) با دستور زیر اجرا کنید:
    ```bash
    streamlit run app.py
    ```

اکنون برنامه در مرورگر شما باز خواهد شد و آماده استفاده است!

## 📄 لایسنس

این پروژه تحت لایسنس MIT منتشر شده است. برای اطلاعات بیشتر فایل `LICENSE` را مطالعه کنید.

</div>
